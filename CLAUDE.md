@AGENTS.md

## Git & deployment approval

Never run `git push`, `vercel --prod`, or any other deploy/publish command
without first stating exactly what will be pushed/deployed and getting
explicit approval in that turn. Local commits are fine to make freely —
pushing to the remote or deploying is not automatic, no matter how
confident the change looks.

